spring:
  datasource:
    # 1. เปลี่ยน IP กลับมาเป็น localhost (เพื่อให้วิ่งเข้าท่อ DBeaver)
    url: jdbc:postgresql://10.202.10.35:5432/bootcamp_db
    username: dev_c4
    # 2. ต้องใส่ " " ครอบตัวเลขเสมอ ไม่งั้น YAML จะมองเป็นตัวเลขไม่ใช่ข้อความ
    password: "1234"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  server:
    port: 8080